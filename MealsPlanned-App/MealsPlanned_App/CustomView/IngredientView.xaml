<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:Custom="clr-namespace:MealsPlanned_App.CustomControl;assembly=MealsPlanned_App"
             x:Class="MealsPlanned_App.CustomView.IngredientView">

    <StackLayout Orientation="Vertical" 
                                             Padding="15,10" >

        <StackLayout Orientation="Horizontal">

            <Custom:QuantityLabel Text="{Binding Quantity}" />

            <Custom:DescriptionLabel Text="{Binding UnityOfMeasureName}" />

            <Custom:DescriptionLabel Text="{Binding Name}" />

            <Custom:DescriptionLabel Text="{Binding Observation}" />
        </StackLayout>

    </StackLayout>
    
</ContentView>